import { Tabs } from 'nextra/components'

# Meta-tag quickstart

Add branded Open Graph images to any page on your site with one meta tag. No server code, no build step.

## 1. Create a project

Sign in at [ogstack.dev](https://ogstack.dev), go to **Projects**, and create a project. Add the domain your pages are served from (for example, `example.com`). OGStack rejects image requests for URLs that don't match an allowlisted domain.

## 2. Copy the public ID

Each project has a public ID like `cLx8kZ9m`. It's safe to expose — domain allowlisting and rate limits prevent abuse. You'll find it on the project detail page.

## 3. Add the meta tag

Pick your framework. Replace `{publicId}` with your project's public ID and the example URL with your page URL.

<Tabs items={['HTML', 'Next.js', 'Nuxt 4', 'Astro', 'SvelteKit', 'Remix', 'Vue 3', 'React (Vite)', 'Angular']}>
<Tabs.Tab>
```html
<meta
  property="og:image"
  content="https://api.ogstack.dev/og/{publicId}?url=https://example.com/blog/post"
/>
<meta name="twitter:card" content="summary_large_image" />
```
</Tabs.Tab>
<Tabs.Tab>
```tsx
// app/page.tsx (or any route)
import type { Metadata } from "next";

export async function generateMetadata({
  params,
}: {
  params: { slug: string };
}): Promise<Metadata> {
  const pageUrl = `https://example.com/${params.slug}`;
  const ogImage = `https://api.ogstack.dev/og/{publicId}?url=${encodeURIComponent(pageUrl)}`;
  return {
    openGraph: { images: [{ url: ogImage, width: 1200, height: 630 }] },
    twitter: { card: "summary_large_image", images: [ogImage] },
  };
}
```
</Tabs.Tab>
<Tabs.Tab>
```vue
<script setup lang="ts">
const route = useRoute();
const ogImage = `https://api.ogstack.dev/og/{publicId}?url=${encodeURIComponent(`https://example.com${route.path}`)}`;

useSeoMeta({
  ogImage,
  twitterCard: "summary_large_image",
});
</script>
```
</Tabs.Tab>
<Tabs.Tab>
```astro
---
const pageUrl = Astro.url.toString();
const ogImage = `https://api.ogstack.dev/og/{publicId}?url=${encodeURIComponent(pageUrl)}`;
---
<meta property="og:image" content={ogImage} />
<meta name="twitter:card" content="summary_large_image" />
```
</Tabs.Tab>
<Tabs.Tab>
```svelte
<script lang="ts">
  import { page } from '$app/stores';
  $: ogImage = `https://api.ogstack.dev/og/{publicId}?url=${encodeURIComponent($page.url.href)}`;
</script>

<svelte:head>
  <meta property="og:image" content={ogImage} />
  <meta name="twitter:card" content="summary_large_image" />
</svelte:head>
```
</Tabs.Tab>
<Tabs.Tab>
```tsx
// app/routes/posts.$slug.tsx
import type { MetaFunction } from "@remix-run/node";

export const meta: MetaFunction = ({ params }) => {
  const pageUrl = `https://example.com/posts/${params.slug}`;
  const ogImage = `https://api.ogstack.dev/og/{publicId}?url=${encodeURIComponent(pageUrl)}`;
  return [
    { property: "og:image", content: ogImage },
    { name: "twitter:card", content: "summary_large_image" },
  ];
};
```
</Tabs.Tab>
<Tabs.Tab>
```vue
<script setup lang="ts">
import { useHead } from "@unhead/vue";
import { useRoute } from "vue-router";

const route = useRoute();
const ogImage = `https://api.ogstack.dev/og/{publicId}?url=${encodeURIComponent(`https://example.com${route.path}`)}`;

useHead({
  meta: [
    { property: "og:image", content: ogImage },
    { name: "twitter:card", content: "summary_large_image" },
  ],
});
</script>
```
</Tabs.Tab>
<Tabs.Tab>
```tsx
import { Helmet } from "react-helmet-async";

export function PostMeta({ slug }: { slug: string }) {
  const pageUrl = `https://example.com/${slug}`;
  const ogImage = `https://api.ogstack.dev/og/{publicId}?url=${encodeURIComponent(pageUrl)}`;
  return (
    <Helmet>
      <meta property="og:image" content={ogImage} />
      <meta name="twitter:card" content="summary_large_image" />
    </Helmet>
  );
}
```
</Tabs.Tab>
<Tabs.Tab>
```html
<!-- src/index.html (inside <head>) -->
<meta
  property="og:image"
  content="https://api.ogstack.dev/og/{publicId}?url=https://example.com/blog/post"
/>
<meta name="twitter:card" content="summary_large_image" />
```

For per-page OG images, pair this with Angular Universal (SSR) so crawlers receive
the rendered tag with the page-specific URL templated server-side.
</Tabs.Tab>
</Tabs>

## 4. Validate

Paste the page URL into the [OG Audit](https://ogstack.dev/audit) tool. You'll see previews for X, Facebook, LinkedIn, Slack, and more, plus a score with actionable fixes.

## Customizing the image

All customization happens in query string parameters. The full list lives in [Guides → OG images](/guides/og-images). A common example:

```html
<meta
  property="og:image"
  content="https://api.ogstack.dev/og/{publicId}?url=https://example.com/post&template=editorial&accent=%2310b981&dark=true"
/>
```

When you need more control than query parameters allow — dynamic prompts, auth-gated pages, or batch generation — use the [API quickstart](/quickstart/api) instead.
